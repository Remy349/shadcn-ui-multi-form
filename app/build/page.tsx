import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Build",
};

export default function Page() {
  return (
    <section>
      <div>
        <h1>Builder Page</h1>
      </div>
    </section>
  );
}
